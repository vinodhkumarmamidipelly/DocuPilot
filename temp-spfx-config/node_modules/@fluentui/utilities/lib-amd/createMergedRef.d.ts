import * as React from 'react';
/**
 * Helper to merge refs from within class components.
 */
export declare const createMergedRef: <TType, TValue = null>(value?: TValue) => (...newRefs: (React.Ref<TType | null | TValue> | undefined)[]) => ((newValue: TType | TValue | null) => void);
